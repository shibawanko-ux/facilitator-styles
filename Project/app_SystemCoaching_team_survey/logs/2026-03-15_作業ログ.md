# 作業ログ 2026-03-15

## [作業] 指示
- 折れ線グラフのX軸を「第N回事前/事後」から実際の日付に変更（Google Analytics風）

## [作業] 作業内容
- `TimelinePoint` インターフェースに `date: string` を追加
- surveys クエリに `created_at` を追加
- `timelineData.push` に日付フォーマット（例：3/14）を追加
- XAxis の `dataKey` を `label` → `date` に変更
- カスタムツールチップを実装（日付・ラベル・スコアを表示）
- グラフ表示条件を `timeline.length > 1` → `timeline.length > 0` に緩和
- 未使用の `Legend` インポートを削除

## 変更・作成ファイル
| 操作 | ファイル |
|------|---------|
| 変更 | src/pages/ResultsPage.tsx |

## 次回やること
- ローカルで実際のデータを入れて動作確認
